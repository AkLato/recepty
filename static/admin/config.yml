backend:
  name: git-gateway
  branch: v2

media_folder: static/assets
public_folder: assets

collections:
  - name: "recepty"
    label: "Recepty"
    folder: "src/posts/gatsby"
    create: true
    slug: "{{slug}}"
    editor:
      preview: false
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Body", name: "body", widget: "markdown" }
